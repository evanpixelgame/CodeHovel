import PrintButton from "./LogThis";
import PrintAllButton from "./LogAll";

const LogButtons = () => {
  return (
    <div className="log-buttons">
      <PrintButton />
      <PrintAllButton />
    </div>
  );
};
export default LogButtons;
